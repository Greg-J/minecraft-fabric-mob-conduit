# Widens the two Display accessors the shared core's hologram code calls. Vanilla keeps both
# private; the Fabric build sees them public because Fabric API's access wideners are applied
# to its compile-time Minecraft (that's also why .minecraft-src shows them public). NeoForge
# compiles against true vanilla, so the equivalent widening is declared here instead.
# Auto-detected by ModDevGradle for compilation and auto-loaded by FML at runtime.
public net.minecraft.world.entity.Display setBillboardConstraints(Lnet/minecraft/world/entity/Display$BillboardConstraints;)V
public net.minecraft.world.entity.Display$TextDisplay setText(Lnet/minecraft/network/chat/Component;)V
